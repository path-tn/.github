# .github

Public org-level defaults and the **public PATH-TN organization profile**.

- [`profile/README.md`](profile/README.md) — renders as the public landing page at
  [github.com/path-tn](https://github.com/path-tn) for non-members and logged-out visitors.

This page intentionally uses **only PATH-TN-owned assets** (logo, colors). Partner logos are kept
off the public profile until reuse rights are confirmed; the full partner strip lives in the
members-only `.github-private` repo.
